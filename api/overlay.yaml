overlay: 1.0.0
info:
  title: MySkoda Public API code generation fixes
  version: 1.0.0
actions:
  - target: $.components.schemas.Charging.properties.tings
    description: Remove a recursive write-only property emitted by the upstream schema.
    remove: true
  - target: $.components.schemas.ChargingProfile.properties.tings
    description: Remove a recursive write-only property emitted by the upstream schema.
    remove: true
